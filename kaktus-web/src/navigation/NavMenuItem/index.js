import dashboard from './dashboard';
import admin from './admin';
import operator from './operator';
import customer from './customer';

const navMenuItem = {
  items: [dashboard, admin, operator, customer]
};

export default navMenuItem;
